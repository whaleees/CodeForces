#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    scanf("%d",&t);
    while(t--){
        string n;
        scanf("%s",&n);
        for(int i = 0; i < strlen(s); i++){
            
        }
    }
}
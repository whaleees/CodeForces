#include <stdio.h>

int main(){
    int tc;
    scanf("%d",&tc);
    while(tc--){
        int n,k,x;
        scanf("%d%d%d",&n,&k,&x);
        int angka[n];
        for(int i = 0; i < n; i++){
            scanf("%d",&angka[i]);
        }
    }
}